<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= todo.title %></title>
</head>
<body>

  <h1><%= todo.title %></h1>

  <hr>

  <a href="/remove-todo/<%= todo.id %>">
    odebrat
  </a>
  &nbsp;
  <a href="/toggle-todo/<%= todo.id %>">
    <%= todo.done ? 'hotovo' : 'nehotvo' %>
  </a>

  <hr>

  <form method="post" action="/update-todo/<%= todo.id %>">
    <h3>Upravit todočko</h3>
    <input placeholder="Název todočka" name="title">
    <br>
    <br>

    <select name="priority">
      <option value="normal" <%= todo.priority === 'normal' ? 'selected' : '' %>><%= getCzechPriorityNameEjs('normal') %></option>
      <option value="low" <%= todo.priority === 'low' ? 'selected' : '' %>><%= getCzechPriorityNameEjs('low') %></option>
      <option value="high" <%= todo.priority === 'high' ? 'selected' : '' %>><%= getCzechPriorityNameEjs('high') %></option>
    </select>
    <br>
    <br>

    <button type="submit">Upravit</button>
  </form>

  <hr>
  
  <a href="/">Zpět na seznam</a>

</body>
</html>
